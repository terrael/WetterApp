


import { Autocomplete } from "@mui/material";

const staedte = [{label: "Berlin", value: "Berlin"}, {label: "Paris", value: "Paris"}];

/** 
const auswahl = () => {
    return <Autocomplete options={staedte} value={(stadt) => staedte.find((stadt.value) === auswahl)}
}*/